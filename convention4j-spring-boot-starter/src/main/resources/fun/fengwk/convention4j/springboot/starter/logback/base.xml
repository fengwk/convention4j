<?xml version="1.0" encoding="UTF-8"?>

<!--
Base logback configuration provided for compatibility with Spring Boot 1.1
-->

<included>
    <!-- ttl MDC -->
    <contextListener class="com.ofpay.logback.TtlMdcListener"/>

	<springProperty scope="context" name="LOG_LEVEL" source="logging.level.root" defaultValue="info"/>

    <!-- 向日志中添加trace记录 -->
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%X{trace}%n%wEx"/>
    <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p ${PID:- } --- [%t] %-40.40logger{39} : %m%X{trace}%n%wEx"/>

	<include resource="org/springframework/boot/logging/logback/defaults.xml" />
	<include resource="org/springframework/boot/logging/logback/console-appender.xml" />
	<include resource="fun/fengwk/convention4j/springboot/starter/logback/file-appender.xml" />

	<root level="${LOG_LEVEL}">
		<!-- 仅在指定测试环境下开启控制台输出 -->
		<springProfile name="default,dev,test">
			<appender-ref ref="CONSOLE"/>
		</springProfile>
		<appender-ref ref="ASYNC_ALL"/>
		<appender-ref ref="ASYNC_ERROR"/>
	</root>
</included>
